apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ghost
spec:
  storageClassName: linode-block-storage-retain
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
