apiVersion: v1
kind: LimitRange
metadata:
  name: limit
spec:
  limits:
  - type: Container
    defaultRequest:
      cpu: 5m
      memory: 25Mi
    default:
      cpu: 30m
      # peak usage of the application controller may be very high
      memory: 250Mi